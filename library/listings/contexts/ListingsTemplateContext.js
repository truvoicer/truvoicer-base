import React from 'react'

export const listingsTemplateData = {
    layout: null,
    paginationComponent: null,
    infiniteScrollComponent: null,
};

export const ListingsTemplateContext = React.createContext(listingsTemplateData);
